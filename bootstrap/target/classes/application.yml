server:
  address: 0.0.0.0
  port: 8080
  compression:
    enabled: true
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://51.38.190.134:3306/extia
    username: app
    password: BabyT3@cher
    hikari:
      connection-timeout: 30000
      minimum-idle: 1
      maximum-pool-size: 10
      idle-timeout: 60000
      max-lifetime: 1800000
      pool-name: extiashare-hikari-pool
