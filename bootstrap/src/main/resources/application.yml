server:
  address: 0.0.0.0
  port: 8080
  compression:
    enabled: true
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://34.163.151.196:5432/extia-hackathon-4a
    username: postgres
    password: extiahackaton
    hikari:
      connection-timeout: 30000
      minimum-idle: 1
      maximum-pool-size: 5
      idle-timeout: 60000
      max-lifetime: 1800000
      pool-name: extiashare-hikari-pool
